#include<stdio.h>
void main()
{
    /*int a,b,n,x,y,z,count=0;

    scanf("%d",&n);

    for(int i=0;i<n;i++){
        scanf("%d %d %d",&x,&y,&z);
        //if(x&y || y&z || x&z) continue;  //bitwise pari na
        if((x==0 && y==0)||(y==0 && z==0)||(x==0 && z==0)) continue;
        else count++;
    }

    printf("Solved: %d\n",count);
    if(count>(n/2)) printf("yes\n");
    else printf("no\n"); */

        printf("\"Thank you ASTHA IT\"");
}
